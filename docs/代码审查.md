## 2026-07-07 15:13

## 代码审查报告

### 审查目标
- 类型：本地改动
- 范围：Workspace 记忆默认存储策略、后台记忆路由提示词、README 配置说明、相关测试
- 审查时间：2026-07-07 15:13

### 审查输入证据
- Diff 范围：`README.md`, `src/config.ts`, `src/constants.ts`, `src/project.ts`, `src/types.ts`, `tests/config.test.ts`, `tests/project.test.ts`
- 自动化检查：`npm run check` 通过；`npm test` 通过
- 设计约束：Workspace 记忆应默认存放在 `<workspace-root>/.pi/`，随项目在 macOS/Windows 间移动；legacy central 模式仍需可配置保留

### 问题清单

#### 严重问题
无。

#### 建议修改
无。

#### 可选优化
无。

### 亮点
- 默认配置和无参项目检测均切换到 `repo-local`，避免新安装机器继续写入 `~/.pi/agent/projects-memory/<project>/`。
- `detectProject("projects-memory", cwd)` 和显式 `"central"` 配置仍保留 legacy central 兼容路径。
- 后台 review 提示词新增强制 scope routing，降低项目事实误写入 global memory 的概率。
- 新增回归测试覆盖默认 repo-local 行为，能防止后续回退。

### 结论
- 结论：`Approved`
- 严重问题：0 个
- 建议修改：0 个
- 可选优化：0 个
- 下一步建议：提交并推送本次改动。

---
